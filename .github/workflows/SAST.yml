name: AI SAST Scan

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai-sast:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: AI SAST Scan
        uses: actions/github-script@v7
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        with:
          script: |
            const fs = require("fs");

            if (!process.env.OPENAI_API_KEY) {
              throw new Error("OPENAI_API_KEY is not set");
            }

            // Read very small app.js (first 50 lines)
            const code = fs.readFileSync("src/app.js", "utf8")
              .split("\n")
              .slice(0, 50)
              .join("\n");

            const prompt = `
            Perform a Static Application Security Testing (SAST) analysis.
            Identify vulnerabilities with:
            - Vulnerability name
            - Severity (Low / Medium / High)
            - Explanation
            - Recommended remediation

            Code:
            ${code}
            `;

            const response = await fetch("https://api.openai.com/v1/chat/completions", {
              method: "POST",
              headers: {
                "Authorization": `Bearer ${process.env.OPENAI_API_KEY}`,
                "Content-Type": "application/json"
              },
              body: JSON.stringify({
                model: "gpt-3.5-turbo",
                messages: [{ role: "user", content: prompt }],
                temperature: 0
              })
            });

            const data = await response.json();

            console.log("OpenAI raw response:", JSON.stringify(data, null, 2));

            // Handle quota errors gracefully
            if (data.error) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `⚠️ AI SAST skipped: ${data.error.message}`
              });
              return;
            }

            if (!data.choices || !data.choices.length) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: "⚠️ AI SAST did not generate any vulnerabilities."
              });
              return;
            }

            const report = data.choices[0].message?.content || "No vulnerabilities found.";

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `### 🤖 AI SAST Report\n${report}`
            });
